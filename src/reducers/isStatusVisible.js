import * as Types from '../actions/types';
import initialState from './initialState';

export default (state = initialState.isStatusVisible, action) => {
  switch (action.type) {
    // case MessagesTypes.FORWARD:
    //   return action.payload;
  }

  return state;
}
