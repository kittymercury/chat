import * as Types from '../../actions/types';
import initialState from '../initialState';

export default (state = initialState.pages.chats, action) => {
  switch (action.type) {
    
  }

  return state;
}
